{
  "name": "exhibition-order-simple",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check app.js && node --check scripts/import-products.mjs && node --check scripts/set-staff-passwords.mjs && node --check scripts/production-snapshot.mjs && node --check scripts/audit-public-repo.mjs",
    "test": "node --test tests/*.test.mjs",
    "security:audit": "node scripts/audit-public-repo.mjs",
    "products:import": "node scripts/import-products.mjs",
    "staff:passwords": "node scripts/set-staff-passwords.mjs",
    "production:snapshot": "node scripts/production-snapshot.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.4",
    "html2canvas": "1.4.1",
    "jspdf": "4.2.1"
  },
  "devDependencies": {
    "dotenv": "17.4.2",
    "supabase": "2.116.0",
    "vite": "8.2.2"
  }
}
