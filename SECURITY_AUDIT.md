# セキュリティ移行監査

調査日: 2026-09-01〜2026-09-02

## 修正前の調査結果

| 対象 | 結果 | 判断 |
|---|---|---|
| JSON | `products.json` 717,196 bytes、7,980商品。品番・商品名・卸価格を含む | 公開不可。GitHubから分離 |
| CSV | リポジトリ内になし。元CSVはデスクトップ上 | GitHubへ追加しない |
| Excel | リポジトリ内になし | GitHubへ追加しない |
| 画像 | `assets/sun_nishimura_logo.jpg` 1点 | 一般公開済みロゴとして残す |
| ハードコード価格 | アプリコードにはなし。`products.json` に卸価格あり | JSONを削除 |
| APIキー／パスワード | 検出なし | 再発行不要 |
| 顧客情報 | 実データなし。入力ラベルと例示だけ | 注文中はメモリだけで扱う |
| localStorage | 展示会名・スタッフ名の端末設定だけ | 新版は展示会名だけに縮小 |
| 注文履歴 | 保存処理なし | 現状維持 |

## Git履歴

- 通常のブランチ／コミット／remoteは存在しませんでした。
- Codexデスクトップが作業差分保護に使う内部 `refs/codex/turn-diffs/...` はありますが、通常の `git push origin main` では公開されません。
- 公開開始時は、非公開ファイルを除去した現在の状態から最初の通常コミットを作成してください。`git push --mirror` は内部参照も対象にし得るため使用しないでください。
- API Secret、service role key、パスワードの過去コミットは確認されず、現時点でキー再発行は不要です。

既存の公開リポジトリに機密ファイルがコミット済みの場合は、現在のファイル削除だけでは不十分です。別途バックアップ後、例えば `git filter-repo --path products.json --invert-paths` で全履歴から除去し、保護ブランチと共同作業者を確認してforce-pushします。公開済みSecretは履歴除去に加えて必ず失効・再発行します。

## 修正後の保存場所

| 保存場所 | 内容 |
|---|---|
| GitHub | UIコード、公開ロゴ、RLSマイグレーション、取込スクリプト、テスト、空の環境変数例 |
| Supabase | Authユーザー、`exhibition_staff`、`products`、`exhibition_accounts` |
| ブラウザメモリ | ログイン後に取得した商品、入力中の顧客・注文・備考、印刷プレビュー |
| sessionStorage | Supabase Authセッションのみ。タブ終了で消去 |
| localStorage | 展示会名のみ |

Supabaseには注文・顧客・注文履歴を保存するテーブルを作っていません。

## 実施済み確認

- Shift-JIS元CSVをドライランし、7,980件を検証
- `products.json` と旧JSON生成スクリプトを削除
- `.env`、CSV、Excel、注文出力フォルダを `.gitignore` へ追加
- 既存Supabaseの3表を確認し、RLSと明示的なGRANT／REVOKEをマイグレーション化
- 未ログイン時に商品問合せを行わない実装
- 商品取得列を品番・商品名・卸価格に限定
- 注文・顧客のlocalStorage保存がないことを確認
- 390×844と1024×768で商品入力、国内通常注文、現売り4種、条件入力、プレビューの表示を確認
- JavaScript構文、単体テスト、公開前監査、Vite本番ビルドに成功

## 接続先Supabaseの事前確認

参照元ツールと一致する `korea-exibition-sannishimura` を読み取り専用で確認しました。

- `products`: 3,861行（有効3,859行、異なる有効品番3,858件）
- `exhibition_staff`: 4行
- `exhibition_accounts`: 8行
- 既存3表はすべてRLS有効
- `anon` は3表とも権限なし。実際に `anon` ロールのSELECTが拒否されることを確認
- `authenticated` でもユーザーJWTなしでは商品0件
- 有効スタッフの既存ポリシーでは3,861行が見えるため、新マイグレーションで `is_active = true` もRLS条件へ追加
- 同じプロジェクトに既存注文22行があるため、注文関連テーブルを一切変更しないことをdry-runで確認
- 既存マイグレーション履歴を同期し、新規 `20260901061650_secure_products.sql` だけが適用対象になるdry-runに成功

## 承認後に実施した本番移行・公開

- 承認されたプロジェクトへRLSマイグレーションを適用
- CSVの7,980商品を取り込み、有効化。旧商品は削除せず非アクティブで保持
- 既存の有効スタッフ4名のパスワードを承認値へ変更。管理用スクリプトで4名全員の認証・商品取得を確認
- 既存注文22件は移行前後で件数・内容の照合値が一致。既存注文関連テーブル・既存ツールは変更なし
- 新規の別リポジトリ `exhibition-order-simple` を作成し、GitHub Pagesで公開
- 公開先: https://masuda8105-prog.github.io/exhibition-order-simple/
- 公開画面は390×844・1024×768で横はみ出しなし。公開バンドルの読込みとコンソールエラーなしを確認
- 公開先へのスタッフ認証情報のブラウザ入力は行っていない（認証試験は管理用スクリプトで実施）

## 最終確認・残る現地確認

- 2026-09-04: 公開先のHTTP 200、本番アセット配信、構文・単体テスト・公開前監査を再確認
- 印刷画面終了時に注文データと入力・プレビューのDOMを消去する処理を追加。ブラウザでは印刷成功とキャンセルを識別できないため両方で消去し、画面上にも明記
- 実プリンターの接続・用紙・余白・排紙は利用端末で最終確認が必要
