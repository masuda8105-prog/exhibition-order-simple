# These two values are embedded in the browser bundle and are public by design.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Local product import only. Never use the VITE_ prefix for this key.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
