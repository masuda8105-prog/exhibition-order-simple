# 2026-09-08 動作確認

## 2026-09-14 Slack共有・お渡し番号

- 公開前の履歴再確認で、別作業による `unique_pickup_numbers` マイグレーション（20260914050502）が追加されていたため、一度公開を停止。全注文対象のNEO採番がSIMPLE試験にも作動し、NEO sequenceが13まで進んだ（発行済み注文は1〜3のまま）。巻き戻しによる重複を避けるためリセットしていない。
- ユーザーの展示会別分離の指示を受け、`isolate_exhibition_pickup_numbering`（20260914053306）を適用。NEOトリガーからSIMPLE注文を除外し、JEX専用カウンターと独立。分離後の2つのDB試験を並行で実行し、NEOカウンターが13のまま、JEXカウンター0・予約0、既存注文数も不変であることを確認。JEX試験注文にNEO番号が付かないこともSQLで検証した。
- 指定された国内ツールのSlack共有パネルの文言・チェック・共有ボタン・日時表示を流用。後日受取／ホテル／指定先配送に表示。チェックは入力確定時、またはプレビューで切り替えた時に既存の共有保存・競合検知を通す。自動Slack投稿はしない。
- 後日受取のみ「JEX-1…」を表示。受付番号と独立し、サーバー採番列から取得。控え、入力画面、注文一覧、番号検索に対応。番号未発行の後日受取は印刷前に保存を求める。
- DB試験SQLで有効スタッフによる連番採番、番号指定拒否、受取日・共有・受け渡し方法の編集後も同一番号、ホテル配送の対象外、注文削除後も番号予約保持、未認証の採番拒否を確認。2つの試験呼出しも並行実行して成功。すべてROLLBACKし、本番番号カウンター0・予約0を確認。試験で既存注文のIDを更新しない。
- ローカルUIでJEX-1→JEX-2の採番、共有チェックの保存、受け取り待ち／お渡し完了、受取日変更後の同一番号、JEX番号検索、ホテル・指定先配送パネルと番号非表示を確認。
- Chromiumの320×568／390×844／760×900／1024×768で横はみ出し・共有チェックとボタンの重なりなし。ロゴ読込み確認。お渡し番号は印刷領域内、Slack操作欄は印刷対象外。
- 11件の単体テスト・構文検査・公開前監査・ビルド成功。実iPhone、実プリンター、実端末同士での同時登録・Slack投稿は未検証。

## 2026-09-14 検索候補の価格表示

- 品番・商品名検索の各候補に、取得済みの商品単価を「卸単価 ¥…」として追加。価格データの保存場所・認証・注文保存処理は変更しない。
- ローカルサンプルで商品名検索と完全一致品番検索の価格表示、および候補からの商品追加を確認。サンプル商品価格は0円。
- Chromiumで320×568、390×844、1024×768を確認。横はみ出しなし、金額と追加ボタンの重なりなし。iPhone実機は未検証。
- 9件の単体テスト、構文検査、公開前監査、ビルド成功。

## 2026-09-11 修正と追加確認

- 検索一覧を固定入力キーの下へ移動。390×844で「1→4→1」の連続入力前後のキー上端219px・下端522pxが変わらず、検索一覧が530pxから表示されることを確認。
- 送料ボタン追加後も320×568、390×844、430×400、760×900、1024×768で横はみ出しなし。本文と固定フッターの境界が一致し、検索一覧がキーより下にあることを計測。
- ホテル名・宿泊者名を任意化。ローカル試験注文で両方空欄のままプレビュー作成に成功。
- 固定入力キーに「＋ 送料500円」を追加。手動で一注文に一回だけ加算し、商品点数から除外、明細の×で削除可能。2回押しても商品5,200円＋送料500円＝5,700円となることを確認。共有データへの変換・復元も単体テストで確認（本番注文は変更しない）。
- 上記追加後、9件の単体テスト・構文検査・公開前監査・ビルド成功。ロゴもプレビューで読込み完了を再確認。
- 公開用ビルドでロゴを正式な画像アセットとして出力。プレビューで読込み完了・実画像幅1054pxを確認。印刷はロゴの読込み完了を待って開始する。
- 注文書のヘッダー・フッターから固定の展示会名を削除。
- 商品入力を上部ヘッダー・スクロール領域・固定フッターへ分割。320×568、390×844、430×400、760×900、1024×768で本文とフッターの境界が一致し、重ならないことを計測。
- 日本語の商品名検索で候補が表示されることを確認。
- 通信エラーによる自動ログアウトを廃止。再接続画面でセッションを保持。明示ログアウトはその端末のセッションだけを終了。
- ホーム画面用のマニフェストを追加。iOSホーム画面アプリの初回ログイン後の実機再起動試験は未実施。
- 注文番号の短縮表示は日付とID先頭8文字を使用。保存済みの内部IDは変更しない。表示用短縮番号自体の一意性制約はなく、編集・同期では完全なUUIDを使用する。

## 確認済み

- 後日受取の入力画面を幅320/360/390/430/600/760/1024pxで計測。日付欄・入力欄・操作ボタンの横はみ出しなし。
- 320px幅でホテル配送のチェックアウト日、指定先配送、国内通常注文と「その他」の帳合先入力を確認。
- ローカル試験注文の備考・受取日を変更し、保存後に一覧から開き直して反映を確認。
- 固定キー操作後、品番入力欄がフォーカスを持たないことを確認。
- スマホの控えで会社名がロゴに圧迫される箇所を修正。印刷時は横配置を維持。
- 有効スタッフの権限でSIMPLE版注文の作成・参照・更新が成功し、古い更新日時では更新0件になることをSQLトランザクション内で検証。試験はROLLBACKし、注文を残さない。
- SIMPLE版専用イベント名の注文が既存国内ツールのイベント名検索に出ないことを検証。
- 6件の単体テスト、構文検査、公開前データ監査、ビルド成功。

## 検証範囲

画面試験はChromium系のブラウザによる幅変更試験。iPhone Safari実機のキーボード・日付選択UI、実プリンターでの排紙は未検証。
印刷ボタン押下後にページ内容が残ることは確認したが、内蔵ブラウザでは標準印刷ダイアログを操作できなかったため、実ダイアログのキャンセル操作は未検証。
控えは公開中の国内ツールの注文書マークアップ・印刷指定を参照して調整。PDFファイル同士の画像差分検証は未実施。
複数の実端末を用いた認証付き同期試験は未実施。データベース権限試験、Realtime登録、ポーリング処理を確認。
## 2026-09-15 控えの会計方法・海外英語表示・候補連続追加

- 現売りの控えに会計方法を追加。現金／クレジットを表示し、未設定の古い値はクレジットと誤表示しない。国内通常注文には既定の会計方法を印字しない。
- 「海外」の場合だけ控えの固定見出し・支払い・受け渡し・送料を英語化。国内へ戻すと日本語へ戻る。商品名、顧客情報、スタッフ名、備考は原文を維持。金額はJPY、日時は日本時間。注文保存形式は変更なし。
- 控えの「ご案内」定型文を削除。入力された備考は残す。
- 候補タップは検索文字・候補DOM・フォーカスを維持し、複数候補を連続追加可能。同一商品は数量加算。Enter・固定キーの完全一致追加は従来どおり検索をクリアする。商品入力のスクロールアンカーによる移動を抑制。
- 架空の893-1／893-2を使う自動テストと、ローカルサンプルD001／D002を使うブラウザ試験で連続追加を確認。本番商品・注文は試験で更新しない。
- Chromiumの390px幅で連続追加、320px幅で英語控えの横はみ出しなし、1024px幅で控え・会計方法の表示とロゴ読込みを確認。海外現金→海外クレジット→国内クレジットの編集・再表示を確認。
- 印刷ボタン押下後も控えが残ることを確認。印刷用DOMに英語・会計方法を反映。実プリンター出力、実iPhone、生成PDFファイルの画像検証は未実施。
- 14件の自動テスト・構文検査・公開前監査・ビルド成功。
## 2026-09-15 Slack共有必須・番号発行と注文確定の分離

- 後日受取は「お渡し番号を発行→Slack共有確認→注文確定」、ホテル・指定先配送は「共有用控え作成→Slack共有確認→注文確定」。国内通常注文／その場渡しは対象外。
- 発行時は既存の保存処理とサーバー採番で一時保存。`submissionState` / `confirmedAt` を既存JSON payloadに保持。未共有では確定ボタンと次注文ボタンを無効化し、確定処理でも再検証。共有チェック単体では未確定のまま。
- 未確定注文は一覧で黄色の表示と「共有・確定を続ける」を表示。中断・再開、共有済み未確定の再表示、チェック解除後の確定禁止をローカルUIで確認。
- 商品／数量／備考／受取日／配送先／会計方法などの変更時は共有確認をリセット。番号は維持。会計済み・お渡し済みだけの操作では再共有不要。旧共有済み注文の確定状態は既存データを書換えず扱う。
- Slackは手動投稿。ボタンはブラウザ印刷／PDF保存であり、投稿を自動検証・自動送信しないことを画面に明記。
- ローカルUIでJEX-1発行→共有→確定→備考編集→再共有必須（JEX-1不変）、ホテル配送の確定、共有取消、その場渡しの従来操作を確認。320／390／760／1024px幅で横はみ出しなし。操作パネルは既存の印刷除外対象。
- 保存失敗／同時編集競合で未確定状態を戻し、成功表示を出さずエラーを表示する自動試験を追加。17件の自動試験・構文検査・公開前監査・ビルド成功。
- Supabaseは読み取り専用でJSON列の型／制約、RLS、JEXとNEOの採番トリガー分離を確認。新規DBマイグレーションなし、本番注文・番号カウンター変更なし。実端末同士の同期、実Slack投稿、実プリンターは未実施。
## 2026-09-16 会社控え・お客様控えの2部印刷

- 全注文の通常印刷で会社控え→お客様控えの2部を同じ内容・同じ番号で生成。両控えの境界で強制改ページ。上部の枠付き見出しと下部に控えの用途を記載。
- 会社控えは顧客区分によらず日本語。お客様控えは国内なら日本語・海外なら英語。商品名・顧客入力値・備考は原文維持。元注文の顧客区分は変更しない。
- Slack用「会社控えをPDF保存（日本語）」は会社控えのみ。通常「2部を印刷」は会社・お客様の両方へ明示的に戻す。双方のロゴ読込みを待って印刷開始し、afterprintで出力モードを解除。
- 全注文区分の2部生成、言語、番号・金額の一致、共有用→通常印刷のモード切替を含め19自動テスト成功。構文検査・公開前監査・ビルド成功。
- ローカルUIで海外後日受取の会社日本語／お客様英語、国内へ戻して両方日本語を確認。320／390／1024pxで横はみ出しなし。両ロゴ読込み・用途中見出し・本文・金額欄を目視確認。
- 内蔵ブラウザの印刷ボタン起動・出力モードと改ページCSSを確認。内蔵ブラウザで実印刷ダイアログ／PDFファイル出力の操作はできないため、生成PDFのページ数・画像検証と実プリンターは未実施。長い明細は各控えが複数ページになる（2部＝常に2ページではない）。
- 本番注文、認証、同期、採番、確定手順は変更なし。
